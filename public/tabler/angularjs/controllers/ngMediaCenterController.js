if (ngTablerApp) {
    ngTablerApp.controller('ngMediaCenterController', [
        '$ngMediaCenterService', '$scope', ($ngMediaCenterService, $scope) => {
            $scope.controllerVariables = {
                title: "Hello"
            }

            let initController = function () {

            }
            initController();

            $scope.controllerFuctions = {}
        }
    ])
}
